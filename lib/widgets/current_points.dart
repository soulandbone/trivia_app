import 'package:flutter/material.dart';

class CurrentPoints extends StatelessWidget {
  const CurrentPoints({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('The current number of points is p-o-i-n-t-s'),
    );
  }
}
